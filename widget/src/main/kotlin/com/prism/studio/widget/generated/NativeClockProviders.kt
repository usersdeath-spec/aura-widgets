package com.prism.studio.widget.generated

import com.prism.studio.widget.NativeWidgetProvider

/**
 * One provider per design, generated from the catalog.
 *
 * Android's widget picker lists one row per declared provider, so this is what makes every design
 * browsable from the home screen rather than only inside our app. Each has its own label and its
 * own previewLayout, so the picker shows the actual widget.
 *
 * Generated by tools/generate_native_widgets.py. Do not edit.
 */

/** Gradient Flow Clock clock. */
class GradientFlowClockHeroProvider : NativeWidgetProvider()

/** Gradient Flow Stacked Clock clock. */
class GradientFlowClockStackedProvider : NativeWidgetProvider()

/** Gradient Flow Clock &amp; Date clock. */
class GradientFlowClockDatedProvider : NativeWidgetProvider()

/** Gradient Flow Clock Banner clock. */
class GradientFlowClockBannerProvider : NativeWidgetProvider()

/** Gradient Flow Clock Seconds clock. */
class GradientFlowClockSecondsProvider : NativeWidgetProvider()

/** Gradient Flow Date clock. */
class GradientFlowDateCardProvider : NativeWidgetProvider()

/** Gradient Flow Month clock. */
class GradientFlowMonthYearProvider : NativeWidgetProvider()

/** Gradient Flow World Clock clock. */
class GradientFlowWorldDualProvider : NativeWidgetProvider()

/** Gradient Flow World Clock 3 clock. */
class GradientFlowWorldTrioProvider : NativeWidgetProvider()

/** Gradient Flow Clock AM/PM clock. */
class GradientFlowClockAmpmProvider : NativeWidgetProvider()

/** Gradient Flow Day of Year clock. */
class GradientFlowYearProgressProvider : NativeWidgetProvider()

/** Gradient Flow Elapsed clock. */
class GradientFlowStopwatchProvider : NativeWidgetProvider()

/** Gradient Flow Date Strip clock. */
class GradientFlowDateWideProvider : NativeWidgetProvider()

/** Sunset Fade Clock clock. */
class SunsetFadeClockHeroProvider : NativeWidgetProvider()

/** Sunset Fade Stacked Clock clock. */
class SunsetFadeClockStackedProvider : NativeWidgetProvider()

/** Sunset Fade Clock &amp; Date clock. */
class SunsetFadeClockDatedProvider : NativeWidgetProvider()

/** Sunset Fade Clock Banner clock. */
class SunsetFadeClockBannerProvider : NativeWidgetProvider()

/** Sunset Fade Clock Seconds clock. */
class SunsetFadeClockSecondsProvider : NativeWidgetProvider()

/** Sunset Fade Date clock. */
class SunsetFadeDateCardProvider : NativeWidgetProvider()

/** Sunset Fade Month clock. */
class SunsetFadeMonthYearProvider : NativeWidgetProvider()

/** Sunset Fade World Clock clock. */
class SunsetFadeWorldDualProvider : NativeWidgetProvider()

/** Sunset Fade World Clock 3 clock. */
class SunsetFadeWorldTrioProvider : NativeWidgetProvider()

/** Sunset Fade Clock AM/PM clock. */
class SunsetFadeClockAmpmProvider : NativeWidgetProvider()

/** Sunset Fade Day of Year clock. */
class SunsetFadeYearProgressProvider : NativeWidgetProvider()

/** Sunset Fade Elapsed clock. */
class SunsetFadeStopwatchProvider : NativeWidgetProvider()

/** Sunset Fade Date Strip clock. */
class SunsetFadeDateWideProvider : NativeWidgetProvider()

/** Candy Pop Clock clock. */
class CandyPopClockHeroProvider : NativeWidgetProvider()

/** Candy Pop Stacked Clock clock. */
class CandyPopClockStackedProvider : NativeWidgetProvider()

/** Candy Pop Clock &amp; Date clock. */
class CandyPopClockDatedProvider : NativeWidgetProvider()

/** Candy Pop Clock Banner clock. */
class CandyPopClockBannerProvider : NativeWidgetProvider()

/** Candy Pop Clock Seconds clock. */
class CandyPopClockSecondsProvider : NativeWidgetProvider()

/** Candy Pop Date clock. */
class CandyPopDateCardProvider : NativeWidgetProvider()

/** Candy Pop Month clock. */
class CandyPopMonthYearProvider : NativeWidgetProvider()

/** Candy Pop World Clock clock. */
class CandyPopWorldDualProvider : NativeWidgetProvider()

/** Candy Pop World Clock 3 clock. */
class CandyPopWorldTrioProvider : NativeWidgetProvider()

/** Candy Pop Clock AM/PM clock. */
class CandyPopClockAmpmProvider : NativeWidgetProvider()

/** Candy Pop Day of Year clock. */
class CandyPopYearProgressProvider : NativeWidgetProvider()

/** Candy Pop Elapsed clock. */
class CandyPopStopwatchProvider : NativeWidgetProvider()

/** Candy Pop Date Strip clock. */
class CandyPopDateWideProvider : NativeWidgetProvider()

/** Terracotta Clock clock. */
class TerracottaClockHeroProvider : NativeWidgetProvider()

/** Terracotta Stacked Clock clock. */
class TerracottaClockStackedProvider : NativeWidgetProvider()

/** Terracotta Clock &amp; Date clock. */
class TerracottaClockDatedProvider : NativeWidgetProvider()

/** Terracotta Clock Banner clock. */
class TerracottaClockBannerProvider : NativeWidgetProvider()

/** Terracotta Clock Seconds clock. */
class TerracottaClockSecondsProvider : NativeWidgetProvider()

/** Terracotta Date clock. */
class TerracottaDateCardProvider : NativeWidgetProvider()

/** Terracotta Month clock. */
class TerracottaMonthYearProvider : NativeWidgetProvider()

/** Terracotta World Clock clock. */
class TerracottaWorldDualProvider : NativeWidgetProvider()

/** Terracotta World Clock 3 clock. */
class TerracottaWorldTrioProvider : NativeWidgetProvider()

/** Terracotta Clock AM/PM clock. */
class TerracottaClockAmpmProvider : NativeWidgetProvider()

/** Terracotta Day of Year clock. */
class TerracottaYearProgressProvider : NativeWidgetProvider()

/** Terracotta Elapsed clock. */
class TerracottaStopwatchProvider : NativeWidgetProvider()

/** Terracotta Date Strip clock. */
class TerracottaDateWideProvider : NativeWidgetProvider()

/** Nordic Frost Clock clock. */
class NordicFrostClockHeroProvider : NativeWidgetProvider()

/** Nordic Frost Stacked Clock clock. */
class NordicFrostClockStackedProvider : NativeWidgetProvider()

/** Nordic Frost Clock &amp; Date clock. */
class NordicFrostClockDatedProvider : NativeWidgetProvider()

/** Nordic Frost Clock Banner clock. */
class NordicFrostClockBannerProvider : NativeWidgetProvider()

/** Nordic Frost Clock Seconds clock. */
class NordicFrostClockSecondsProvider : NativeWidgetProvider()

/** Nordic Frost Date clock. */
class NordicFrostDateCardProvider : NativeWidgetProvider()

/** Nordic Frost Month clock. */
class NordicFrostMonthYearProvider : NativeWidgetProvider()

/** Nordic Frost World Clock clock. */
class NordicFrostWorldDualProvider : NativeWidgetProvider()

/** Nordic Frost World Clock 3 clock. */
class NordicFrostWorldTrioProvider : NativeWidgetProvider()

/** Nordic Frost Clock AM/PM clock. */
class NordicFrostClockAmpmProvider : NativeWidgetProvider()

/** Nordic Frost Day of Year clock. */
class NordicFrostYearProgressProvider : NativeWidgetProvider()

/** Nordic Frost Elapsed clock. */
class NordicFrostStopwatchProvider : NativeWidgetProvider()

/** Nordic Frost Date Strip clock. */
class NordicFrostDateWideProvider : NativeWidgetProvider()

/** Seasonal Bloom Clock clock. */
class SeasonalBloomClockHeroProvider : NativeWidgetProvider()

/** Seasonal Bloom Stacked Clock clock. */
class SeasonalBloomClockStackedProvider : NativeWidgetProvider()

/** Seasonal Bloom Clock &amp; Date clock. */
class SeasonalBloomClockDatedProvider : NativeWidgetProvider()

/** Seasonal Bloom Clock Banner clock. */
class SeasonalBloomClockBannerProvider : NativeWidgetProvider()

/** Seasonal Bloom Clock Seconds clock. */
class SeasonalBloomClockSecondsProvider : NativeWidgetProvider()

/** Seasonal Bloom Date clock. */
class SeasonalBloomDateCardProvider : NativeWidgetProvider()

/** Seasonal Bloom Month clock. */
class SeasonalBloomMonthYearProvider : NativeWidgetProvider()

/** Seasonal Bloom World Clock clock. */
class SeasonalBloomWorldDualProvider : NativeWidgetProvider()

/** Seasonal Bloom World Clock 3 clock. */
class SeasonalBloomWorldTrioProvider : NativeWidgetProvider()

/** Seasonal Bloom Clock AM/PM clock. */
class SeasonalBloomClockAmpmProvider : NativeWidgetProvider()

/** Seasonal Bloom Day of Year clock. */
class SeasonalBloomYearProgressProvider : NativeWidgetProvider()

/** Seasonal Bloom Elapsed clock. */
class SeasonalBloomStopwatchProvider : NativeWidgetProvider()

/** Seasonal Bloom Date Strip clock. */
class SeasonalBloomDateWideProvider : NativeWidgetProvider()

/** Deep Space Clock clock. */
class DeepSpaceClockHeroProvider : NativeWidgetProvider()

/** Deep Space Stacked Clock clock. */
class DeepSpaceClockStackedProvider : NativeWidgetProvider()

/** Deep Space Clock &amp; Date clock. */
class DeepSpaceClockDatedProvider : NativeWidgetProvider()

/** Deep Space Clock Banner clock. */
class DeepSpaceClockBannerProvider : NativeWidgetProvider()

/** Deep Space Clock Seconds clock. */
class DeepSpaceClockSecondsProvider : NativeWidgetProvider()

/** Deep Space Date clock. */
class DeepSpaceDateCardProvider : NativeWidgetProvider()

/** Deep Space Month clock. */
class DeepSpaceMonthYearProvider : NativeWidgetProvider()

/** Deep Space World Clock clock. */
class DeepSpaceWorldDualProvider : NativeWidgetProvider()

/** Deep Space World Clock 3 clock. */
class DeepSpaceWorldTrioProvider : NativeWidgetProvider()

/** Deep Space Clock AM/PM clock. */
class DeepSpaceClockAmpmProvider : NativeWidgetProvider()

/** Deep Space Day of Year clock. */
class DeepSpaceYearProgressProvider : NativeWidgetProvider()

/** Deep Space Elapsed clock. */
class DeepSpaceStopwatchProvider : NativeWidgetProvider()

/** Deep Space Date Strip clock. */
class DeepSpaceDateWideProvider : NativeWidgetProvider()

/** Monolith Clock clock. */
class MonolithClockHeroProvider : NativeWidgetProvider()

/** Monolith Stacked Clock clock. */
class MonolithClockStackedProvider : NativeWidgetProvider()

/** Monolith Clock &amp; Date clock. */
class MonolithClockDatedProvider : NativeWidgetProvider()

/** Monolith Clock Banner clock. */
class MonolithClockBannerProvider : NativeWidgetProvider()

/** Monolith Clock Seconds clock. */
class MonolithClockSecondsProvider : NativeWidgetProvider()

/** Monolith Date clock. */
class MonolithDateCardProvider : NativeWidgetProvider()

/** Monolith Month clock. */
class MonolithMonthYearProvider : NativeWidgetProvider()

/** Monolith World Clock clock. */
class MonolithWorldDualProvider : NativeWidgetProvider()

/** Monolith World Clock 3 clock. */
class MonolithWorldTrioProvider : NativeWidgetProvider()

/** Monolith Clock AM/PM clock. */
class MonolithClockAmpmProvider : NativeWidgetProvider()

/** Monolith Day of Year clock. */
class MonolithYearProgressProvider : NativeWidgetProvider()

/** Monolith Elapsed clock. */
class MonolithStopwatchProvider : NativeWidgetProvider()

/** Monolith Date Strip clock. */
class MonolithDateWideProvider : NativeWidgetProvider()

/** AMOLED Black Clock clock. */
class AmoledBlackClockHeroProvider : NativeWidgetProvider()

/** AMOLED Black Stacked Clock clock. */
class AmoledBlackClockStackedProvider : NativeWidgetProvider()

/** AMOLED Black Clock &amp; Date clock. */
class AmoledBlackClockDatedProvider : NativeWidgetProvider()

/** AMOLED Black Clock Banner clock. */
class AmoledBlackClockBannerProvider : NativeWidgetProvider()

/** AMOLED Black Clock Seconds clock. */
class AmoledBlackClockSecondsProvider : NativeWidgetProvider()

/** AMOLED Black Date clock. */
class AmoledBlackDateCardProvider : NativeWidgetProvider()

/** AMOLED Black Month clock. */
class AmoledBlackMonthYearProvider : NativeWidgetProvider()

/** AMOLED Black World Clock clock. */
class AmoledBlackWorldDualProvider : NativeWidgetProvider()

/** AMOLED Black World Clock 3 clock. */
class AmoledBlackWorldTrioProvider : NativeWidgetProvider()

/** AMOLED Black Clock AM/PM clock. */
class AmoledBlackClockAmpmProvider : NativeWidgetProvider()

/** AMOLED Black Day of Year clock. */
class AmoledBlackYearProgressProvider : NativeWidgetProvider()

/** AMOLED Black Elapsed clock. */
class AmoledBlackStopwatchProvider : NativeWidgetProvider()

/** AMOLED Black Date Strip clock. */
class AmoledBlackDateWideProvider : NativeWidgetProvider()

/** Luxury Gold Clock clock. */
class LuxuryGoldClockHeroProvider : NativeWidgetProvider()

/** Luxury Gold Stacked Clock clock. */
class LuxuryGoldClockStackedProvider : NativeWidgetProvider()

/** Luxury Gold Clock &amp; Date clock. */
class LuxuryGoldClockDatedProvider : NativeWidgetProvider()

/** Luxury Gold Clock Banner clock. */
class LuxuryGoldClockBannerProvider : NativeWidgetProvider()

/** Luxury Gold Clock Seconds clock. */
class LuxuryGoldClockSecondsProvider : NativeWidgetProvider()

/** Luxury Gold Date clock. */
class LuxuryGoldDateCardProvider : NativeWidgetProvider()

/** Luxury Gold Month clock. */
class LuxuryGoldMonthYearProvider : NativeWidgetProvider()

/** Luxury Gold World Clock clock. */
class LuxuryGoldWorldDualProvider : NativeWidgetProvider()

/** Luxury Gold World Clock 3 clock. */
class LuxuryGoldWorldTrioProvider : NativeWidgetProvider()

/** Luxury Gold Clock AM/PM clock. */
class LuxuryGoldClockAmpmProvider : NativeWidgetProvider()

/** Luxury Gold Day of Year clock. */
class LuxuryGoldYearProgressProvider : NativeWidgetProvider()

/** Luxury Gold Elapsed clock. */
class LuxuryGoldStopwatchProvider : NativeWidgetProvider()

/** Luxury Gold Date Strip clock. */
class LuxuryGoldDateWideProvider : NativeWidgetProvider()

/** Chrome Liquid Clock clock. */
class ChromeLiquidClockHeroProvider : NativeWidgetProvider()

/** Chrome Liquid Stacked Clock clock. */
class ChromeLiquidClockStackedProvider : NativeWidgetProvider()

/** Chrome Liquid Clock &amp; Date clock. */
class ChromeLiquidClockDatedProvider : NativeWidgetProvider()

/** Chrome Liquid Clock Banner clock. */
class ChromeLiquidClockBannerProvider : NativeWidgetProvider()

/** Chrome Liquid Clock Seconds clock. */
class ChromeLiquidClockSecondsProvider : NativeWidgetProvider()

/** Chrome Liquid Date clock. */
class ChromeLiquidDateCardProvider : NativeWidgetProvider()

/** Chrome Liquid Month clock. */
class ChromeLiquidMonthYearProvider : NativeWidgetProvider()

/** Chrome Liquid World Clock clock. */
class ChromeLiquidWorldDualProvider : NativeWidgetProvider()

/** Chrome Liquid World Clock 3 clock. */
class ChromeLiquidWorldTrioProvider : NativeWidgetProvider()

/** Chrome Liquid Clock AM/PM clock. */
class ChromeLiquidClockAmpmProvider : NativeWidgetProvider()

/** Chrome Liquid Day of Year clock. */
class ChromeLiquidYearProgressProvider : NativeWidgetProvider()

/** Chrome Liquid Elapsed clock. */
class ChromeLiquidStopwatchProvider : NativeWidgetProvider()

/** Chrome Liquid Date Strip clock. */
class ChromeLiquidDateWideProvider : NativeWidgetProvider()

/** Origami Clock clock. */
class OrigamiClockHeroProvider : NativeWidgetProvider()

/** Origami Stacked Clock clock. */
class OrigamiClockStackedProvider : NativeWidgetProvider()

/** Origami Clock &amp; Date clock. */
class OrigamiClockDatedProvider : NativeWidgetProvider()

/** Origami Clock Banner clock. */
class OrigamiClockBannerProvider : NativeWidgetProvider()

/** Origami Clock Seconds clock. */
class OrigamiClockSecondsProvider : NativeWidgetProvider()

/** Origami Date clock. */
class OrigamiDateCardProvider : NativeWidgetProvider()

/** Origami Month clock. */
class OrigamiMonthYearProvider : NativeWidgetProvider()

/** Origami World Clock clock. */
class OrigamiWorldDualProvider : NativeWidgetProvider()

/** Origami World Clock 3 clock. */
class OrigamiWorldTrioProvider : NativeWidgetProvider()

/** Origami Clock AM/PM clock. */
class OrigamiClockAmpmProvider : NativeWidgetProvider()

/** Origami Day of Year clock. */
class OrigamiYearProgressProvider : NativeWidgetProvider()

/** Origami Elapsed clock. */
class OrigamiStopwatchProvider : NativeWidgetProvider()

/** Origami Date Strip clock. */
class OrigamiDateWideProvider : NativeWidgetProvider()

/** Paper Cut Clock clock. */
class PaperCutClockHeroProvider : NativeWidgetProvider()

/** Paper Cut Stacked Clock clock. */
class PaperCutClockStackedProvider : NativeWidgetProvider()

/** Paper Cut Clock &amp; Date clock. */
class PaperCutClockDatedProvider : NativeWidgetProvider()

/** Paper Cut Clock Banner clock. */
class PaperCutClockBannerProvider : NativeWidgetProvider()

/** Paper Cut Clock Seconds clock. */
class PaperCutClockSecondsProvider : NativeWidgetProvider()

/** Paper Cut Date clock. */
class PaperCutDateCardProvider : NativeWidgetProvider()

/** Paper Cut Month clock. */
class PaperCutMonthYearProvider : NativeWidgetProvider()

/** Paper Cut World Clock clock. */
class PaperCutWorldDualProvider : NativeWidgetProvider()

/** Paper Cut World Clock 3 clock. */
class PaperCutWorldTrioProvider : NativeWidgetProvider()

/** Paper Cut Clock AM/PM clock. */
class PaperCutClockAmpmProvider : NativeWidgetProvider()

/** Paper Cut Day of Year clock. */
class PaperCutYearProgressProvider : NativeWidgetProvider()

/** Paper Cut Elapsed clock. */
class PaperCutStopwatchProvider : NativeWidgetProvider()

/** Paper Cut Date Strip clock. */
class PaperCutDateWideProvider : NativeWidgetProvider()

/** Titanium Clock clock. */
class TitaniumClockHeroProvider : NativeWidgetProvider()

/** Titanium Stacked Clock clock. */
class TitaniumClockStackedProvider : NativeWidgetProvider()

/** Titanium Clock &amp; Date clock. */
class TitaniumClockDatedProvider : NativeWidgetProvider()

/** Titanium Clock Banner clock. */
class TitaniumClockBannerProvider : NativeWidgetProvider()

/** Titanium Clock Seconds clock. */
class TitaniumClockSecondsProvider : NativeWidgetProvider()

/** Titanium Date clock. */
class TitaniumDateCardProvider : NativeWidgetProvider()

/** Titanium Month clock. */
class TitaniumMonthYearProvider : NativeWidgetProvider()

/** Titanium World Clock clock. */
class TitaniumWorldDualProvider : NativeWidgetProvider()

/** Titanium World Clock 3 clock. */
class TitaniumWorldTrioProvider : NativeWidgetProvider()

/** Titanium Clock AM/PM clock. */
class TitaniumClockAmpmProvider : NativeWidgetProvider()

/** Titanium Day of Year clock. */
class TitaniumYearProgressProvider : NativeWidgetProvider()

/** Titanium Elapsed clock. */
class TitaniumStopwatchProvider : NativeWidgetProvider()

/** Titanium Date Strip clock. */
class TitaniumDateWideProvider : NativeWidgetProvider()

/** Mercury Clock clock. */
class MercuryClockHeroProvider : NativeWidgetProvider()

/** Mercury Stacked Clock clock. */
class MercuryClockStackedProvider : NativeWidgetProvider()

/** Mercury Clock &amp; Date clock. */
class MercuryClockDatedProvider : NativeWidgetProvider()

/** Mercury Clock Banner clock. */
class MercuryClockBannerProvider : NativeWidgetProvider()

/** Mercury Clock Seconds clock. */
class MercuryClockSecondsProvider : NativeWidgetProvider()

/** Mercury Date clock. */
class MercuryDateCardProvider : NativeWidgetProvider()

/** Mercury Month clock. */
class MercuryMonthYearProvider : NativeWidgetProvider()

/** Mercury World Clock clock. */
class MercuryWorldDualProvider : NativeWidgetProvider()

/** Mercury World Clock 3 clock. */
class MercuryWorldTrioProvider : NativeWidgetProvider()

/** Mercury Clock AM/PM clock. */
class MercuryClockAmpmProvider : NativeWidgetProvider()

/** Mercury Day of Year clock. */
class MercuryYearProgressProvider : NativeWidgetProvider()

/** Mercury Elapsed clock. */
class MercuryStopwatchProvider : NativeWidgetProvider()

/** Mercury Date Strip clock. */
class MercuryDateWideProvider : NativeWidgetProvider()

/** Quartz Clock clock. */
class QuartzClockHeroProvider : NativeWidgetProvider()

/** Quartz Stacked Clock clock. */
class QuartzClockStackedProvider : NativeWidgetProvider()

/** Quartz Clock &amp; Date clock. */
class QuartzClockDatedProvider : NativeWidgetProvider()

/** Quartz Clock Banner clock. */
class QuartzClockBannerProvider : NativeWidgetProvider()

/** Quartz Clock Seconds clock. */
class QuartzClockSecondsProvider : NativeWidgetProvider()

/** Quartz Date clock. */
class QuartzDateCardProvider : NativeWidgetProvider()

/** Quartz Month clock. */
class QuartzMonthYearProvider : NativeWidgetProvider()

/** Quartz World Clock clock. */
class QuartzWorldDualProvider : NativeWidgetProvider()

/** Quartz World Clock 3 clock. */
class QuartzWorldTrioProvider : NativeWidgetProvider()

/** Quartz Clock AM/PM clock. */
class QuartzClockAmpmProvider : NativeWidgetProvider()

/** Quartz Day of Year clock. */
class QuartzYearProgressProvider : NativeWidgetProvider()

/** Quartz Elapsed clock. */
class QuartzStopwatchProvider : NativeWidgetProvider()

/** Quartz Date Strip clock. */
class QuartzDateWideProvider : NativeWidgetProvider()

/** Velvet Clock clock. */
class VelvetClockHeroProvider : NativeWidgetProvider()

/** Velvet Stacked Clock clock. */
class VelvetClockStackedProvider : NativeWidgetProvider()

/** Velvet Clock &amp; Date clock. */
class VelvetClockDatedProvider : NativeWidgetProvider()

/** Velvet Clock Banner clock. */
class VelvetClockBannerProvider : NativeWidgetProvider()

/** Velvet Clock Seconds clock. */
class VelvetClockSecondsProvider : NativeWidgetProvider()

/** Velvet Date clock. */
class VelvetDateCardProvider : NativeWidgetProvider()

/** Velvet Month clock. */
class VelvetMonthYearProvider : NativeWidgetProvider()

/** Velvet World Clock clock. */
class VelvetWorldDualProvider : NativeWidgetProvider()

/** Velvet World Clock 3 clock. */
class VelvetWorldTrioProvider : NativeWidgetProvider()

/** Velvet Clock AM/PM clock. */
class VelvetClockAmpmProvider : NativeWidgetProvider()

/** Velvet Day of Year clock. */
class VelvetYearProgressProvider : NativeWidgetProvider()

/** Velvet Elapsed clock. */
class VelvetStopwatchProvider : NativeWidgetProvider()

/** Velvet Date Strip clock. */
class VelvetDateWideProvider : NativeWidgetProvider()

/** Obsidian Clock clock. */
class ObsidianClockHeroProvider : NativeWidgetProvider()

/** Obsidian Stacked Clock clock. */
class ObsidianClockStackedProvider : NativeWidgetProvider()

/** Obsidian Clock &amp; Date clock. */
class ObsidianClockDatedProvider : NativeWidgetProvider()

/** Obsidian Clock Banner clock. */
class ObsidianClockBannerProvider : NativeWidgetProvider()

/** Obsidian Clock Seconds clock. */
class ObsidianClockSecondsProvider : NativeWidgetProvider()

/** Obsidian Date clock. */
class ObsidianDateCardProvider : NativeWidgetProvider()

/** Obsidian Month clock. */
class ObsidianMonthYearProvider : NativeWidgetProvider()

/** Obsidian World Clock clock. */
class ObsidianWorldDualProvider : NativeWidgetProvider()

/** Obsidian World Clock 3 clock. */
class ObsidianWorldTrioProvider : NativeWidgetProvider()

/** Obsidian Clock AM/PM clock. */
class ObsidianClockAmpmProvider : NativeWidgetProvider()

/** Obsidian Day of Year clock. */
class ObsidianYearProgressProvider : NativeWidgetProvider()

/** Obsidian Elapsed clock. */
class ObsidianStopwatchProvider : NativeWidgetProvider()

/** Obsidian Date Strip clock. */
class ObsidianDateWideProvider : NativeWidgetProvider()

/** Pearl Clock clock. */
class PearlClockHeroProvider : NativeWidgetProvider()

/** Pearl Stacked Clock clock. */
class PearlClockStackedProvider : NativeWidgetProvider()

/** Pearl Clock &amp; Date clock. */
class PearlClockDatedProvider : NativeWidgetProvider()

/** Pearl Clock Banner clock. */
class PearlClockBannerProvider : NativeWidgetProvider()

/** Pearl Clock Seconds clock. */
class PearlClockSecondsProvider : NativeWidgetProvider()

/** Pearl Date clock. */
class PearlDateCardProvider : NativeWidgetProvider()

/** Pearl Month clock. */
class PearlMonthYearProvider : NativeWidgetProvider()

/** Pearl World Clock clock. */
class PearlWorldDualProvider : NativeWidgetProvider()

/** Pearl World Clock 3 clock. */
class PearlWorldTrioProvider : NativeWidgetProvider()

/** Pearl Clock AM/PM clock. */
class PearlClockAmpmProvider : NativeWidgetProvider()

/** Pearl Day of Year clock. */
class PearlYearProgressProvider : NativeWidgetProvider()

/** Pearl Elapsed clock. */
class PearlStopwatchProvider : NativeWidgetProvider()

/** Pearl Date Strip clock. */
class PearlDateWideProvider : NativeWidgetProvider()

/** Gemstone Clock clock. */
class GemstoneClockHeroProvider : NativeWidgetProvider()

/** Gemstone Stacked Clock clock. */
class GemstoneClockStackedProvider : NativeWidgetProvider()

/** Gemstone Clock &amp; Date clock. */
class GemstoneClockDatedProvider : NativeWidgetProvider()

/** Gemstone Clock Banner clock. */
class GemstoneClockBannerProvider : NativeWidgetProvider()

/** Gemstone Clock Seconds clock. */
class GemstoneClockSecondsProvider : NativeWidgetProvider()

/** Gemstone Date clock. */
class GemstoneDateCardProvider : NativeWidgetProvider()

/** Gemstone Month clock. */
class GemstoneMonthYearProvider : NativeWidgetProvider()

/** Gemstone World Clock clock. */
class GemstoneWorldDualProvider : NativeWidgetProvider()

/** Gemstone World Clock 3 clock. */
class GemstoneWorldTrioProvider : NativeWidgetProvider()

/** Gemstone Clock AM/PM clock. */
class GemstoneClockAmpmProvider : NativeWidgetProvider()

/** Gemstone Day of Year clock. */
class GemstoneYearProgressProvider : NativeWidgetProvider()

/** Gemstone Elapsed clock. */
class GemstoneStopwatchProvider : NativeWidgetProvider()

/** Gemstone Date Strip clock. */
class GemstoneDateWideProvider : NativeWidgetProvider()

/** Holographic Clock clock. */
class HolographicClockHeroProvider : NativeWidgetProvider()

/** Holographic Stacked Clock clock. */
class HolographicClockStackedProvider : NativeWidgetProvider()

/** Holographic Clock &amp; Date clock. */
class HolographicClockDatedProvider : NativeWidgetProvider()

/** Holographic Clock Banner clock. */
class HolographicClockBannerProvider : NativeWidgetProvider()

/** Holographic Clock Seconds clock. */
class HolographicClockSecondsProvider : NativeWidgetProvider()

/** Holographic Date clock. */
class HolographicDateCardProvider : NativeWidgetProvider()

/** Holographic Month clock. */
class HolographicMonthYearProvider : NativeWidgetProvider()

/** Holographic World Clock clock. */
class HolographicWorldDualProvider : NativeWidgetProvider()

/** Holographic World Clock 3 clock. */
class HolographicWorldTrioProvider : NativeWidgetProvider()

/** Holographic Clock AM/PM clock. */
class HolographicClockAmpmProvider : NativeWidgetProvider()

/** Holographic Day of Year clock. */
class HolographicYearProgressProvider : NativeWidgetProvider()

/** Holographic Elapsed clock. */
class HolographicStopwatchProvider : NativeWidgetProvider()

/** Holographic Date Strip clock. */
class HolographicDateWideProvider : NativeWidgetProvider()

/** Eclipse Clock clock. */
class EclipseClockHeroProvider : NativeWidgetProvider()

/** Eclipse Stacked Clock clock. */
class EclipseClockStackedProvider : NativeWidgetProvider()

/** Eclipse Clock &amp; Date clock. */
class EclipseClockDatedProvider : NativeWidgetProvider()

/** Eclipse Clock Banner clock. */
class EclipseClockBannerProvider : NativeWidgetProvider()

/** Eclipse Clock Seconds clock. */
class EclipseClockSecondsProvider : NativeWidgetProvider()

/** Eclipse Date clock. */
class EclipseDateCardProvider : NativeWidgetProvider()

/** Eclipse Month clock. */
class EclipseMonthYearProvider : NativeWidgetProvider()

/** Eclipse World Clock clock. */
class EclipseWorldDualProvider : NativeWidgetProvider()

/** Eclipse World Clock 3 clock. */
class EclipseWorldTrioProvider : NativeWidgetProvider()

/** Eclipse Clock AM/PM clock. */
class EclipseClockAmpmProvider : NativeWidgetProvider()

/** Eclipse Day of Year clock. */
class EclipseYearProgressProvider : NativeWidgetProvider()

/** Eclipse Elapsed clock. */
class EclipseStopwatchProvider : NativeWidgetProvider()

/** Eclipse Date Strip clock. */
class EclipseDateWideProvider : NativeWidgetProvider()

/** Solar Clock clock. */
class SolarClockHeroProvider : NativeWidgetProvider()

/** Solar Stacked Clock clock. */
class SolarClockStackedProvider : NativeWidgetProvider()

/** Solar Clock &amp; Date clock. */
class SolarClockDatedProvider : NativeWidgetProvider()

/** Solar Clock Banner clock. */
class SolarClockBannerProvider : NativeWidgetProvider()

/** Solar Clock Seconds clock. */
class SolarClockSecondsProvider : NativeWidgetProvider()

/** Solar Date clock. */
class SolarDateCardProvider : NativeWidgetProvider()

/** Solar Month clock. */
class SolarMonthYearProvider : NativeWidgetProvider()

/** Solar World Clock clock. */
class SolarWorldDualProvider : NativeWidgetProvider()

/** Solar World Clock 3 clock. */
class SolarWorldTrioProvider : NativeWidgetProvider()

/** Solar Clock AM/PM clock. */
class SolarClockAmpmProvider : NativeWidgetProvider()

/** Solar Day of Year clock. */
class SolarYearProgressProvider : NativeWidgetProvider()

/** Solar Elapsed clock. */
class SolarStopwatchProvider : NativeWidgetProvider()

/** Solar Date Strip clock. */
class SolarDateWideProvider : NativeWidgetProvider()

/** Abyssal Clock clock. */
class AbyssalClockHeroProvider : NativeWidgetProvider()

/** Abyssal Stacked Clock clock. */
class AbyssalClockStackedProvider : NativeWidgetProvider()

/** Abyssal Clock &amp; Date clock. */
class AbyssalClockDatedProvider : NativeWidgetProvider()

/** Abyssal Clock Banner clock. */
class AbyssalClockBannerProvider : NativeWidgetProvider()

/** Abyssal Clock Seconds clock. */
class AbyssalClockSecondsProvider : NativeWidgetProvider()

/** Abyssal Date clock. */
class AbyssalDateCardProvider : NativeWidgetProvider()

/** Abyssal Month clock. */
class AbyssalMonthYearProvider : NativeWidgetProvider()

/** Abyssal World Clock clock. */
class AbyssalWorldDualProvider : NativeWidgetProvider()

/** Abyssal World Clock 3 clock. */
class AbyssalWorldTrioProvider : NativeWidgetProvider()

/** Abyssal Clock AM/PM clock. */
class AbyssalClockAmpmProvider : NativeWidgetProvider()

/** Abyssal Day of Year clock. */
class AbyssalYearProgressProvider : NativeWidgetProvider()

/** Abyssal Elapsed clock. */
class AbyssalStopwatchProvider : NativeWidgetProvider()

/** Abyssal Date Strip clock. */
class AbyssalDateWideProvider : NativeWidgetProvider()

/** Horizon Clock clock. */
class HorizonClockHeroProvider : NativeWidgetProvider()

/** Horizon Stacked Clock clock. */
class HorizonClockStackedProvider : NativeWidgetProvider()

/** Horizon Clock &amp; Date clock. */
class HorizonClockDatedProvider : NativeWidgetProvider()

/** Horizon Clock Banner clock. */
class HorizonClockBannerProvider : NativeWidgetProvider()

/** Horizon Clock Seconds clock. */
class HorizonClockSecondsProvider : NativeWidgetProvider()

/** Horizon Date clock. */
class HorizonDateCardProvider : NativeWidgetProvider()

/** Horizon Month clock. */
class HorizonMonthYearProvider : NativeWidgetProvider()

/** Horizon World Clock clock. */
class HorizonWorldDualProvider : NativeWidgetProvider()

/** Horizon World Clock 3 clock. */
class HorizonWorldTrioProvider : NativeWidgetProvider()

/** Horizon Clock AM/PM clock. */
class HorizonClockAmpmProvider : NativeWidgetProvider()

/** Horizon Day of Year clock. */
class HorizonYearProgressProvider : NativeWidgetProvider()

/** Horizon Elapsed clock. */
class HorizonStopwatchProvider : NativeWidgetProvider()

/** Horizon Date Strip clock. */
class HorizonDateWideProvider : NativeWidgetProvider()

/** Mirage Clock clock. */
class MirageClockHeroProvider : NativeWidgetProvider()

/** Mirage Stacked Clock clock. */
class MirageClockStackedProvider : NativeWidgetProvider()

/** Mirage Clock &amp; Date clock. */
class MirageClockDatedProvider : NativeWidgetProvider()

/** Mirage Clock Banner clock. */
class MirageClockBannerProvider : NativeWidgetProvider()

/** Mirage Clock Seconds clock. */
class MirageClockSecondsProvider : NativeWidgetProvider()

/** Mirage Date clock. */
class MirageDateCardProvider : NativeWidgetProvider()

/** Mirage Month clock. */
class MirageMonthYearProvider : NativeWidgetProvider()

/** Mirage World Clock clock. */
class MirageWorldDualProvider : NativeWidgetProvider()

/** Mirage World Clock 3 clock. */
class MirageWorldTrioProvider : NativeWidgetProvider()

/** Mirage Clock AM/PM clock. */
class MirageClockAmpmProvider : NativeWidgetProvider()

/** Mirage Day of Year clock. */
class MirageYearProgressProvider : NativeWidgetProvider()

/** Mirage Elapsed clock. */
class MirageStopwatchProvider : NativeWidgetProvider()

/** Mirage Date Strip clock. */
class MirageDateWideProvider : NativeWidgetProvider()

/** Studio Clock clock. */
class StudioClockHeroProvider : NativeWidgetProvider()

/** Studio Stacked Clock clock. */
class StudioClockStackedProvider : NativeWidgetProvider()

/** Studio Clock &amp; Date clock. */
class StudioClockDatedProvider : NativeWidgetProvider()

/** Studio Clock Banner clock. */
class StudioClockBannerProvider : NativeWidgetProvider()

/** Studio Clock Seconds clock. */
class StudioClockSecondsProvider : NativeWidgetProvider()

/** Studio Date clock. */
class StudioDateCardProvider : NativeWidgetProvider()

/** Studio Month clock. */
class StudioMonthYearProvider : NativeWidgetProvider()

/** Studio World Clock clock. */
class StudioWorldDualProvider : NativeWidgetProvider()

/** Studio World Clock 3 clock. */
class StudioWorldTrioProvider : NativeWidgetProvider()

/** Studio Clock AM/PM clock. */
class StudioClockAmpmProvider : NativeWidgetProvider()

/** Studio Day of Year clock. */
class StudioYearProgressProvider : NativeWidgetProvider()

/** Studio Elapsed clock. */
class StudioStopwatchProvider : NativeWidgetProvider()

/** Studio Date Strip clock. */
class StudioDateWideProvider : NativeWidgetProvider()

/** Scandinavian Clock clock. */
class ScandinavianClockHeroProvider : NativeWidgetProvider()

/** Scandinavian Stacked Clock clock. */
class ScandinavianClockStackedProvider : NativeWidgetProvider()

/** Scandinavian Clock &amp; Date clock. */
class ScandinavianClockDatedProvider : NativeWidgetProvider()

/** Scandinavian Clock Banner clock. */
class ScandinavianClockBannerProvider : NativeWidgetProvider()

/** Scandinavian Clock Seconds clock. */
class ScandinavianClockSecondsProvider : NativeWidgetProvider()

/** Scandinavian Date clock. */
class ScandinavianDateCardProvider : NativeWidgetProvider()

/** Scandinavian Month clock. */
class ScandinavianMonthYearProvider : NativeWidgetProvider()

/** Scandinavian World Clock clock. */
class ScandinavianWorldDualProvider : NativeWidgetProvider()

/** Scandinavian World Clock 3 clock. */
class ScandinavianWorldTrioProvider : NativeWidgetProvider()

/** Scandinavian Clock AM/PM clock. */
class ScandinavianClockAmpmProvider : NativeWidgetProvider()

/** Scandinavian Day of Year clock. */
class ScandinavianYearProgressProvider : NativeWidgetProvider()

/** Scandinavian Elapsed clock. */
class ScandinavianStopwatchProvider : NativeWidgetProvider()

/** Scandinavian Date Strip clock. */
class ScandinavianDateWideProvider : NativeWidgetProvider()

/** Japanese Zen Clock clock. */
class JapaneseZenClockHeroProvider : NativeWidgetProvider()

/** Japanese Zen Stacked Clock clock. */
class JapaneseZenClockStackedProvider : NativeWidgetProvider()

/** Japanese Zen Clock &amp; Date clock. */
class JapaneseZenClockDatedProvider : NativeWidgetProvider()

/** Japanese Zen Clock Banner clock. */
class JapaneseZenClockBannerProvider : NativeWidgetProvider()

/** Japanese Zen Clock Seconds clock. */
class JapaneseZenClockSecondsProvider : NativeWidgetProvider()

/** Japanese Zen Date clock. */
class JapaneseZenDateCardProvider : NativeWidgetProvider()

/** Japanese Zen Month clock. */
class JapaneseZenMonthYearProvider : NativeWidgetProvider()

/** Japanese Zen World Clock clock. */
class JapaneseZenWorldDualProvider : NativeWidgetProvider()

/** Japanese Zen World Clock 3 clock. */
class JapaneseZenWorldTrioProvider : NativeWidgetProvider()

/** Japanese Zen Clock AM/PM clock. */
class JapaneseZenClockAmpmProvider : NativeWidgetProvider()

/** Japanese Zen Day of Year clock. */
class JapaneseZenYearProgressProvider : NativeWidgetProvider()

/** Japanese Zen Elapsed clock. */
class JapaneseZenStopwatchProvider : NativeWidgetProvider()

/** Japanese Zen Date Strip clock. */
class JapaneseZenDateWideProvider : NativeWidgetProvider()

/** Swiss Grid Clock clock. */
class SwissGridClockHeroProvider : NativeWidgetProvider()

/** Swiss Grid Stacked Clock clock. */
class SwissGridClockStackedProvider : NativeWidgetProvider()

/** Swiss Grid Clock &amp; Date clock. */
class SwissGridClockDatedProvider : NativeWidgetProvider()

/** Swiss Grid Clock Banner clock. */
class SwissGridClockBannerProvider : NativeWidgetProvider()

/** Swiss Grid Clock Seconds clock. */
class SwissGridClockSecondsProvider : NativeWidgetProvider()

/** Swiss Grid Date clock. */
class SwissGridDateCardProvider : NativeWidgetProvider()

/** Swiss Grid Month clock. */
class SwissGridMonthYearProvider : NativeWidgetProvider()

/** Swiss Grid World Clock clock. */
class SwissGridWorldDualProvider : NativeWidgetProvider()

/** Swiss Grid World Clock 3 clock. */
class SwissGridWorldTrioProvider : NativeWidgetProvider()

/** Swiss Grid Clock AM/PM clock. */
class SwissGridClockAmpmProvider : NativeWidgetProvider()

/** Swiss Grid Day of Year clock. */
class SwissGridYearProgressProvider : NativeWidgetProvider()

/** Swiss Grid Elapsed clock. */
class SwissGridStopwatchProvider : NativeWidgetProvider()

/** Swiss Grid Date Strip clock. */
class SwissGridDateWideProvider : NativeWidgetProvider()

/** Bauhaus Primary Clock clock. */
class BauhausPrimaryClockHeroProvider : NativeWidgetProvider()

/** Bauhaus Primary Stacked Clock clock. */
class BauhausPrimaryClockStackedProvider : NativeWidgetProvider()

/** Bauhaus Primary Clock &amp; Date clock. */
class BauhausPrimaryClockDatedProvider : NativeWidgetProvider()

/** Bauhaus Primary Clock Banner clock. */
class BauhausPrimaryClockBannerProvider : NativeWidgetProvider()

/** Bauhaus Primary Clock Seconds clock. */
class BauhausPrimaryClockSecondsProvider : NativeWidgetProvider()

/** Bauhaus Primary Date clock. */
class BauhausPrimaryDateCardProvider : NativeWidgetProvider()

/** Bauhaus Primary Month clock. */
class BauhausPrimaryMonthYearProvider : NativeWidgetProvider()

/** Bauhaus Primary World Clock clock. */
class BauhausPrimaryWorldDualProvider : NativeWidgetProvider()

/** Bauhaus Primary World Clock 3 clock. */
class BauhausPrimaryWorldTrioProvider : NativeWidgetProvider()

/** Bauhaus Primary Clock AM/PM clock. */
class BauhausPrimaryClockAmpmProvider : NativeWidgetProvider()

/** Bauhaus Primary Day of Year clock. */
class BauhausPrimaryYearProgressProvider : NativeWidgetProvider()

/** Bauhaus Primary Elapsed clock. */
class BauhausPrimaryStopwatchProvider : NativeWidgetProvider()

/** Bauhaus Primary Date Strip clock. */
class BauhausPrimaryDateWideProvider : NativeWidgetProvider()

/** Brutalist Slab Clock clock. */
class BrutalistSlabClockHeroProvider : NativeWidgetProvider()

/** Brutalist Slab Stacked Clock clock. */
class BrutalistSlabClockStackedProvider : NativeWidgetProvider()

/** Brutalist Slab Clock &amp; Date clock. */
class BrutalistSlabClockDatedProvider : NativeWidgetProvider()

/** Brutalist Slab Clock Banner clock. */
class BrutalistSlabClockBannerProvider : NativeWidgetProvider()

/** Brutalist Slab Clock Seconds clock. */
class BrutalistSlabClockSecondsProvider : NativeWidgetProvider()

/** Brutalist Slab Date clock. */
class BrutalistSlabDateCardProvider : NativeWidgetProvider()

/** Brutalist Slab Month clock. */
class BrutalistSlabMonthYearProvider : NativeWidgetProvider()

/** Brutalist Slab World Clock clock. */
class BrutalistSlabWorldDualProvider : NativeWidgetProvider()

/** Brutalist Slab World Clock 3 clock. */
class BrutalistSlabWorldTrioProvider : NativeWidgetProvider()

/** Brutalist Slab Clock AM/PM clock. */
class BrutalistSlabClockAmpmProvider : NativeWidgetProvider()

/** Brutalist Slab Day of Year clock. */
class BrutalistSlabYearProgressProvider : NativeWidgetProvider()

/** Brutalist Slab Elapsed clock. */
class BrutalistSlabStopwatchProvider : NativeWidgetProvider()

/** Brutalist Slab Date Strip clock. */
class BrutalistSlabDateWideProvider : NativeWidgetProvider()

/** Ink Serif Clock clock. */
class InkSerifClockHeroProvider : NativeWidgetProvider()

/** Ink Serif Stacked Clock clock. */
class InkSerifClockStackedProvider : NativeWidgetProvider()

/** Ink Serif Clock &amp; Date clock. */
class InkSerifClockDatedProvider : NativeWidgetProvider()

/** Ink Serif Clock Banner clock. */
class InkSerifClockBannerProvider : NativeWidgetProvider()

/** Ink Serif Clock Seconds clock. */
class InkSerifClockSecondsProvider : NativeWidgetProvider()

/** Ink Serif Date clock. */
class InkSerifDateCardProvider : NativeWidgetProvider()

/** Ink Serif Month clock. */
class InkSerifMonthYearProvider : NativeWidgetProvider()

/** Ink Serif World Clock clock. */
class InkSerifWorldDualProvider : NativeWidgetProvider()

/** Ink Serif World Clock 3 clock. */
class InkSerifWorldTrioProvider : NativeWidgetProvider()

/** Ink Serif Clock AM/PM clock. */
class InkSerifClockAmpmProvider : NativeWidgetProvider()

/** Ink Serif Day of Year clock. */
class InkSerifYearProgressProvider : NativeWidgetProvider()

/** Ink Serif Elapsed clock. */
class InkSerifStopwatchProvider : NativeWidgetProvider()

/** Ink Serif Date Strip clock. */
class InkSerifDateWideProvider : NativeWidgetProvider()

/** Cyberpunk Neon Clock clock. */
class CyberpunkNeonClockHeroProvider : NativeWidgetProvider()

/** Cyberpunk Neon Stacked Clock clock. */
class CyberpunkNeonClockStackedProvider : NativeWidgetProvider()

/** Cyberpunk Neon Clock &amp; Date clock. */
class CyberpunkNeonClockDatedProvider : NativeWidgetProvider()

/** Cyberpunk Neon Clock Banner clock. */
class CyberpunkNeonClockBannerProvider : NativeWidgetProvider()

/** Cyberpunk Neon Clock Seconds clock. */
class CyberpunkNeonClockSecondsProvider : NativeWidgetProvider()

/** Cyberpunk Neon Date clock. */
class CyberpunkNeonDateCardProvider : NativeWidgetProvider()

/** Cyberpunk Neon Month clock. */
class CyberpunkNeonMonthYearProvider : NativeWidgetProvider()

/** Cyberpunk Neon World Clock clock. */
class CyberpunkNeonWorldDualProvider : NativeWidgetProvider()

/** Cyberpunk Neon World Clock 3 clock. */
class CyberpunkNeonWorldTrioProvider : NativeWidgetProvider()

/** Cyberpunk Neon Clock AM/PM clock. */
class CyberpunkNeonClockAmpmProvider : NativeWidgetProvider()

/** Cyberpunk Neon Day of Year clock. */
class CyberpunkNeonYearProgressProvider : NativeWidgetProvider()

/** Cyberpunk Neon Elapsed clock. */
class CyberpunkNeonStopwatchProvider : NativeWidgetProvider()

/** Cyberpunk Neon Date Strip clock. */
class CyberpunkNeonDateWideProvider : NativeWidgetProvider()

/** HUD Tactical Clock clock. */
class HudTacticalClockHeroProvider : NativeWidgetProvider()

/** HUD Tactical Stacked Clock clock. */
class HudTacticalClockStackedProvider : NativeWidgetProvider()

/** HUD Tactical Clock &amp; Date clock. */
class HudTacticalClockDatedProvider : NativeWidgetProvider()

/** HUD Tactical Clock Banner clock. */
class HudTacticalClockBannerProvider : NativeWidgetProvider()

/** HUD Tactical Clock Seconds clock. */
class HudTacticalClockSecondsProvider : NativeWidgetProvider()

/** HUD Tactical Date clock. */
class HudTacticalDateCardProvider : NativeWidgetProvider()

/** HUD Tactical Month clock. */
class HudTacticalMonthYearProvider : NativeWidgetProvider()

/** HUD Tactical World Clock clock. */
class HudTacticalWorldDualProvider : NativeWidgetProvider()

/** HUD Tactical World Clock 3 clock. */
class HudTacticalWorldTrioProvider : NativeWidgetProvider()

/** HUD Tactical Clock AM/PM clock. */
class HudTacticalClockAmpmProvider : NativeWidgetProvider()

/** HUD Tactical Day of Year clock. */
class HudTacticalYearProgressProvider : NativeWidgetProvider()

/** HUD Tactical Elapsed clock. */
class HudTacticalStopwatchProvider : NativeWidgetProvider()

/** HUD Tactical Date Strip clock. */
class HudTacticalDateWideProvider : NativeWidgetProvider()

/** RGB Gaming Clock clock. */
class RgbGamingClockHeroProvider : NativeWidgetProvider()

/** RGB Gaming Stacked Clock clock. */
class RgbGamingClockStackedProvider : NativeWidgetProvider()

/** RGB Gaming Clock &amp; Date clock. */
class RgbGamingClockDatedProvider : NativeWidgetProvider()

/** RGB Gaming Clock Banner clock. */
class RgbGamingClockBannerProvider : NativeWidgetProvider()

/** RGB Gaming Clock Seconds clock. */
class RgbGamingClockSecondsProvider : NativeWidgetProvider()

/** RGB Gaming Date clock. */
class RgbGamingDateCardProvider : NativeWidgetProvider()

/** RGB Gaming Month clock. */
class RgbGamingMonthYearProvider : NativeWidgetProvider()

/** RGB Gaming World Clock clock. */
class RgbGamingWorldDualProvider : NativeWidgetProvider()

/** RGB Gaming World Clock 3 clock. */
class RgbGamingWorldTrioProvider : NativeWidgetProvider()

/** RGB Gaming Clock AM/PM clock. */
class RgbGamingClockAmpmProvider : NativeWidgetProvider()

/** RGB Gaming Day of Year clock. */
class RgbGamingYearProgressProvider : NativeWidgetProvider()

/** RGB Gaming Elapsed clock. */
class RgbGamingStopwatchProvider : NativeWidgetProvider()

/** RGB Gaming Date Strip clock. */
class RgbGamingDateWideProvider : NativeWidgetProvider()

/** Pixel Retro Clock clock. */
class PixelRetroClockHeroProvider : NativeWidgetProvider()

/** Pixel Retro Stacked Clock clock. */
class PixelRetroClockStackedProvider : NativeWidgetProvider()

/** Pixel Retro Clock &amp; Date clock. */
class PixelRetroClockDatedProvider : NativeWidgetProvider()

/** Pixel Retro Clock Banner clock. */
class PixelRetroClockBannerProvider : NativeWidgetProvider()

/** Pixel Retro Clock Seconds clock. */
class PixelRetroClockSecondsProvider : NativeWidgetProvider()

/** Pixel Retro Date clock. */
class PixelRetroDateCardProvider : NativeWidgetProvider()

/** Pixel Retro Month clock. */
class PixelRetroMonthYearProvider : NativeWidgetProvider()

/** Pixel Retro World Clock clock. */
class PixelRetroWorldDualProvider : NativeWidgetProvider()

/** Pixel Retro World Clock 3 clock. */
class PixelRetroWorldTrioProvider : NativeWidgetProvider()

/** Pixel Retro Clock AM/PM clock. */
class PixelRetroClockAmpmProvider : NativeWidgetProvider()

/** Pixel Retro Day of Year clock. */
class PixelRetroYearProgressProvider : NativeWidgetProvider()

/** Pixel Retro Elapsed clock. */
class PixelRetroStopwatchProvider : NativeWidgetProvider()

/** Pixel Retro Date Strip clock. */
class PixelRetroDateWideProvider : NativeWidgetProvider()

/** CRT Amber Clock clock. */
class CrtAmberClockHeroProvider : NativeWidgetProvider()

/** CRT Amber Stacked Clock clock. */
class CrtAmberClockStackedProvider : NativeWidgetProvider()

/** CRT Amber Clock &amp; Date clock. */
class CrtAmberClockDatedProvider : NativeWidgetProvider()

/** CRT Amber Clock Banner clock. */
class CrtAmberClockBannerProvider : NativeWidgetProvider()

/** CRT Amber Clock Seconds clock. */
class CrtAmberClockSecondsProvider : NativeWidgetProvider()

/** CRT Amber Date clock. */
class CrtAmberDateCardProvider : NativeWidgetProvider()

/** CRT Amber Month clock. */
class CrtAmberMonthYearProvider : NativeWidgetProvider()

/** CRT Amber World Clock clock. */
class CrtAmberWorldDualProvider : NativeWidgetProvider()

/** CRT Amber World Clock 3 clock. */
class CrtAmberWorldTrioProvider : NativeWidgetProvider()

/** CRT Amber Clock AM/PM clock. */
class CrtAmberClockAmpmProvider : NativeWidgetProvider()

/** CRT Amber Day of Year clock. */
class CrtAmberYearProgressProvider : NativeWidgetProvider()

/** CRT Amber Elapsed clock. */
class CrtAmberStopwatchProvider : NativeWidgetProvider()

/** CRT Amber Date Strip clock. */
class CrtAmberDateWideProvider : NativeWidgetProvider()

/** Blueprint Clock clock. */
class BlueprintClockHeroProvider : NativeWidgetProvider()

/** Blueprint Stacked Clock clock. */
class BlueprintClockStackedProvider : NativeWidgetProvider()

/** Blueprint Clock &amp; Date clock. */
class BlueprintClockDatedProvider : NativeWidgetProvider()

/** Blueprint Clock Banner clock. */
class BlueprintClockBannerProvider : NativeWidgetProvider()

/** Blueprint Clock Seconds clock. */
class BlueprintClockSecondsProvider : NativeWidgetProvider()

/** Blueprint Date clock. */
class BlueprintDateCardProvider : NativeWidgetProvider()

/** Blueprint Month clock. */
class BlueprintMonthYearProvider : NativeWidgetProvider()

/** Blueprint World Clock clock. */
class BlueprintWorldDualProvider : NativeWidgetProvider()

/** Blueprint World Clock 3 clock. */
class BlueprintWorldTrioProvider : NativeWidgetProvider()

/** Blueprint Clock AM/PM clock. */
class BlueprintClockAmpmProvider : NativeWidgetProvider()

/** Blueprint Day of Year clock. */
class BlueprintYearProgressProvider : NativeWidgetProvider()

/** Blueprint Elapsed clock. */
class BlueprintStopwatchProvider : NativeWidgetProvider()

/** Blueprint Date Strip clock. */
class BlueprintDateWideProvider : NativeWidgetProvider()

/** Executive Slate Clock clock. */
class ExecutiveSlateClockHeroProvider : NativeWidgetProvider()

/** Executive Slate Stacked Clock clock. */
class ExecutiveSlateClockStackedProvider : NativeWidgetProvider()

/** Executive Slate Clock &amp; Date clock. */
class ExecutiveSlateClockDatedProvider : NativeWidgetProvider()

/** Executive Slate Clock Banner clock. */
class ExecutiveSlateClockBannerProvider : NativeWidgetProvider()

/** Executive Slate Clock Seconds clock. */
class ExecutiveSlateClockSecondsProvider : NativeWidgetProvider()

/** Executive Slate Date clock. */
class ExecutiveSlateDateCardProvider : NativeWidgetProvider()

/** Executive Slate Month clock. */
class ExecutiveSlateMonthYearProvider : NativeWidgetProvider()

/** Executive Slate World Clock clock. */
class ExecutiveSlateWorldDualProvider : NativeWidgetProvider()

/** Executive Slate World Clock 3 clock. */
class ExecutiveSlateWorldTrioProvider : NativeWidgetProvider()

/** Executive Slate Clock AM/PM clock. */
class ExecutiveSlateClockAmpmProvider : NativeWidgetProvider()

/** Executive Slate Day of Year clock. */
class ExecutiveSlateYearProgressProvider : NativeWidgetProvider()

/** Executive Slate Elapsed clock. */
class ExecutiveSlateStopwatchProvider : NativeWidgetProvider()

/** Executive Slate Date Strip clock. */
class ExecutiveSlateDateWideProvider : NativeWidgetProvider()

/** Ledger Clock clock. */
class LedgerClockHeroProvider : NativeWidgetProvider()

/** Ledger Stacked Clock clock. */
class LedgerClockStackedProvider : NativeWidgetProvider()

/** Ledger Clock &amp; Date clock. */
class LedgerClockDatedProvider : NativeWidgetProvider()

/** Ledger Clock Banner clock. */
class LedgerClockBannerProvider : NativeWidgetProvider()

/** Ledger Clock Seconds clock. */
class LedgerClockSecondsProvider : NativeWidgetProvider()

/** Ledger Date clock. */
class LedgerDateCardProvider : NativeWidgetProvider()

/** Ledger Month clock. */
class LedgerMonthYearProvider : NativeWidgetProvider()

/** Ledger World Clock clock. */
class LedgerWorldDualProvider : NativeWidgetProvider()

/** Ledger World Clock 3 clock. */
class LedgerWorldTrioProvider : NativeWidgetProvider()

/** Ledger Clock AM/PM clock. */
class LedgerClockAmpmProvider : NativeWidgetProvider()

/** Ledger Day of Year clock. */
class LedgerYearProgressProvider : NativeWidgetProvider()

/** Ledger Elapsed clock. */
class LedgerStopwatchProvider : NativeWidgetProvider()

/** Ledger Date Strip clock. */
class LedgerDateWideProvider : NativeWidgetProvider()

/** Pulse Clock clock. */
class PulseClockHeroProvider : NativeWidgetProvider()

/** Pulse Stacked Clock clock. */
class PulseClockStackedProvider : NativeWidgetProvider()

/** Pulse Clock &amp; Date clock. */
class PulseClockDatedProvider : NativeWidgetProvider()

/** Pulse Clock Banner clock. */
class PulseClockBannerProvider : NativeWidgetProvider()

/** Pulse Clock Seconds clock. */
class PulseClockSecondsProvider : NativeWidgetProvider()

/** Pulse Date clock. */
class PulseDateCardProvider : NativeWidgetProvider()

/** Pulse Month clock. */
class PulseMonthYearProvider : NativeWidgetProvider()

/** Pulse World Clock clock. */
class PulseWorldDualProvider : NativeWidgetProvider()

/** Pulse World Clock 3 clock. */
class PulseWorldTrioProvider : NativeWidgetProvider()

/** Pulse Clock AM/PM clock. */
class PulseClockAmpmProvider : NativeWidgetProvider()

/** Pulse Day of Year clock. */
class PulseYearProgressProvider : NativeWidgetProvider()

/** Pulse Elapsed clock. */
class PulseStopwatchProvider : NativeWidgetProvider()

/** Pulse Date Strip clock. */
class PulseDateWideProvider : NativeWidgetProvider()

/** Vinyl Clock clock. */
class VinylClockHeroProvider : NativeWidgetProvider()

/** Vinyl Stacked Clock clock. */
class VinylClockStackedProvider : NativeWidgetProvider()

/** Vinyl Clock &amp; Date clock. */
class VinylClockDatedProvider : NativeWidgetProvider()

/** Vinyl Clock Banner clock. */
class VinylClockBannerProvider : NativeWidgetProvider()

/** Vinyl Clock Seconds clock. */
class VinylClockSecondsProvider : NativeWidgetProvider()

/** Vinyl Date clock. */
class VinylDateCardProvider : NativeWidgetProvider()

/** Vinyl Month clock. */
class VinylMonthYearProvider : NativeWidgetProvider()

/** Vinyl World Clock clock. */
class VinylWorldDualProvider : NativeWidgetProvider()

/** Vinyl World Clock 3 clock. */
class VinylWorldTrioProvider : NativeWidgetProvider()

/** Vinyl Clock AM/PM clock. */
class VinylClockAmpmProvider : NativeWidgetProvider()

/** Vinyl Day of Year clock. */
class VinylYearProgressProvider : NativeWidgetProvider()

/** Vinyl Elapsed clock. */
class VinylStopwatchProvider : NativeWidgetProvider()

/** Vinyl Date Strip clock. */
class VinylDateWideProvider : NativeWidgetProvider()

/** Focus Grid Clock clock. */
class FocusGridClockHeroProvider : NativeWidgetProvider()

/** Focus Grid Stacked Clock clock. */
class FocusGridClockStackedProvider : NativeWidgetProvider()

/** Focus Grid Clock &amp; Date clock. */
class FocusGridClockDatedProvider : NativeWidgetProvider()

/** Focus Grid Clock Banner clock. */
class FocusGridClockBannerProvider : NativeWidgetProvider()

/** Focus Grid Clock Seconds clock. */
class FocusGridClockSecondsProvider : NativeWidgetProvider()

/** Focus Grid Date clock. */
class FocusGridDateCardProvider : NativeWidgetProvider()

/** Focus Grid Month clock. */
class FocusGridMonthYearProvider : NativeWidgetProvider()

/** Focus Grid World Clock clock. */
class FocusGridWorldDualProvider : NativeWidgetProvider()

/** Focus Grid World Clock 3 clock. */
class FocusGridWorldTrioProvider : NativeWidgetProvider()

/** Focus Grid Clock AM/PM clock. */
class FocusGridClockAmpmProvider : NativeWidgetProvider()

/** Focus Grid Day of Year clock. */
class FocusGridYearProgressProvider : NativeWidgetProvider()

/** Focus Grid Elapsed clock. */
class FocusGridStopwatchProvider : NativeWidgetProvider()

/** Focus Grid Date Strip clock. */
class FocusGridDateWideProvider : NativeWidgetProvider()
